/// <reference types="react" />
import { RuleProps } from './types';
export declare const Rule: {
    ({ id, parentId, field, operator, value, translations, schema: { classNames, controls, fields, getInputType, getPlaceHolder, getLevel, getOperators, getValueEditorType, getValues, onPropChange, onRuleRemove, removeIconatStart, autoSelectField }, context }: RuleProps): JSX.Element;
    displayName: string;
};
